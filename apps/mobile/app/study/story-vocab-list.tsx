import type { ReactElement } from 'react';
import { StoryVocabListScreen } from '../../src/learning/card-types/story-reading/story-vocab-list-screen';

export default function StoryVocabListRoute(): ReactElement {
  return <StoryVocabListScreen />;
}
