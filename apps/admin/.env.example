# 复制为 apps/admin/.env（勿提交 Git）
# dev：留空，通过 Vite 代理访问 http://127.0.0.1:3000（见 vite.config.ts）
# 生产：若 Caddy 在 admin 子域反代 /api（见 infra/prod/Caddyfile.example），可留空
# 若 Admin 与 API 不同源且无反代，构建前设置：
# VITE_API_BASE_URL=https://api.remember.wehub.top
