{
  "name": "remember-app",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.13 <23",
    "pnpm": "10.33.2"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "build:packages": "pnpm --filter @remember/contracts build && pnpm --filter @remember/domain build",
    "check:prepare": "pnpm build:packages && pnpm --filter @remember/pack-builder build && node tools/scripts/ensure-prisma-client.mjs",
    "typecheck": "pnpm build:packages && pnpm -r --if-present typecheck",
    "test": "node --test tools/checks/check-source.test.mjs tools/scripts/validate-prod-env.test.mjs && pnpm build:packages && node tools/scripts/ensure-prisma-client.mjs && pnpm -r --if-present test",
    "test:contract": "pnpm --filter @remember/contracts test",
    "test:integration": "pnpm build:packages && pnpm -r --if-present test:integration",
    "mobile:apk:debug": "powershell -NoProfile -ExecutionPolicy Bypass -File tools/mobile/build-standalone-debug-apk.ps1",
    "check:deps": "depcruise --config dependency-cruiser.config.mjs apps packages tools",
    "check:secrets": "secretlint \"**/*\"",
    "check:source": "node tools/checks/check-source.mjs",
    "check:prod-env": "node tools/scripts/validate-prod-env.mjs",
    "dev:db": "docker compose --project-name remember-dev --env-file infra/dev/.env --file infra/dev/compose.yaml up -d",
    "dev:db:down": "docker compose --project-name remember-dev --env-file infra/dev/.env --file infra/dev/compose.yaml down",
    "dev:pack-editor": "pnpm --filter @remember/pack-editor dev",
    "build": "node tools/scripts/ensure-prisma-client.mjs && pnpm -r --if-present build",
    "check": "pnpm format:check && pnpm check:prepare && pnpm lint && pnpm typecheck && pnpm test && pnpm test:contract && pnpm check:deps && pnpm check:secrets && pnpm check:source && pnpm build"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@secretlint/secretlint-rule-preset-recommend": "13.0.4",
    "@types/node": "26.1.1",
    "@vitest/eslint-plugin": "1.6.24",
    "dependency-cruiser": "18.1.0",
    "eslint": "10.8.0",
    "globals": "17.7.0",
    "prettier": "3.9.0",
    "secretlint": "13.0.4",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10"
  }
}
